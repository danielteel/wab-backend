{
  "name": "wab-backend",
  "version": "1.0.0",
  "description": "backend for wab",
  "main": "app.js",
  "scripts": {
    "dev-test": "npm jest",
    "dev-start": "npx nodemon app.js",
    "dev-init": "npx knex migrate:down --env development && npx knex migrate:up --env development && npx knex seed:run --env development",
    "prod-init": "source ./.env && npx knex migrate:down --env production && npx knex migrate:up --env production && npx knex seed:run --env production"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "knex": "^0.95.15",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "sqlite3": "^5.0.2"
  }
}
